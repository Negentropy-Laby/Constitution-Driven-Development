# Product Concept: Tiny Importer

## Core Promise

Help a user validate a CSV file before importing records into a system.

## User / JTBD

When preparing a data import, the user wants to find invalid rows before any data is changed.

## MVP Scope

One CLI command, dry-run validation, row-level error output, and release checklist.
